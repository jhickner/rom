# TODO

- [x] how much influence do we have on the colors? could we do a color
      translation into something resembling the terminal theme? that would be
      interesting
      → done: `--recolor hue|nearest|duotone|dither`, F8 cycles. Terminal
        palette read via OSC 4/10/11; mapping in Oklab through a 64K LUT.
